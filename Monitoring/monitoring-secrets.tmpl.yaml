apiVersion: v1
kind: Secret
metadata:
  name: monitoring-credentials
  namespace: monitoring
type: Opaque
stringData:
  admin-user: admin
  # CircleCI will replace these placeholders with your actual secure environment variables
  grafana-password: "${GRAFANA_ADMIN_PASSWORD}"
  slack-webhook-url: "${SLACK_WEBHOOK_URL}"